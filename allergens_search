from bs4 import BeautifulSoup
import requests
import re


allergens = [ ]   #string list of all allergens to search webpage for

url = ' '  #replace target URL

allergen_length = len(allergens)

#requests sends HTTP GET request to URL 
webpage = requests.get(url)
soup = BeautifulSoup(webpage.text, features="html.parser")

#temp = soup.get_text()

#print(temp)

for word in example:
    allergen_length -= 1
    if word in temp.lower():
        print ("Found: " + word)
        break
        
    elif (allergen_length == 0 ):
        
        print("You're good!")
